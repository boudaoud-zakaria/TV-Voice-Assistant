# Core voice + NLP dependencies
SpeechRecognition>=3.10.0
sentence-transformers>=2.2.2
rapidfuzz>=2.11.0

# Microphone input (requires system 'portaudio': `brew install portaudio` on macOS)
PyAudio>=0.2.14

# These are pulled in automatically by sentence-transformers; pip resolves
# compatible versions for your Python. Listed for reference:
# torch, transformers, numpy, scipy, scikit-learn
